name: loop-and-commit

on:
  pull_request:

jobs:
  loop_and_commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 1 }
      - name: Quick sanity
        run: |
          echo "loop-and-commit PR check"
          test -f CONTEXT.yaml && echo "CONTEXT.yaml exists"
