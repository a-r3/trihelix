# Helix Interface (Human & Agents)

## Entry points
- CLI: `helix start`, `helix loop run`, `helix explain`
- API (future): `/loop`, `/state`, `/insight`

## Guarantees
- Reads CONTEXT.yaml before actions
- Writes memory and logs after actions
